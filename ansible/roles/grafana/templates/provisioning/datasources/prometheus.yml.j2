apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://{{ prometheus_host | default('prometheus-vm') }}:9090
    isDefault: true
    editable: true

